services:
  nodejs:
    image: ayush0827/notecraft:last
    ports:
      - 8000:8000
    env_file:
      - .env
    depends_on:
      - redis
    restart: always
    
  redis : 
    image: redis:7-alpine
    volumes:
      - redis-data:/data
    restart: always
  

volumes:
   redis-data:
